// Thin re-export wrapper to keep imports stable.
// Some files import Button from "../ui/components/Button" while the base
// component lives in Buttons.jsx.

export { Button } from "./Buttons.jsx";
