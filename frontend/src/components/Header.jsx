<header className="sticky top-0 z-20 border-b border-border/70 bg-bg/70 backdrop-blur supports-[backdrop-filter]:bg-bg/60">
  <div className={`${ui.container} flex items-center justify-between py-4`}>
    <Link to="/" className="text-[15px] font-semibold tracking-tight text-text">
      Telegram Shop
    </Link>

    <nav className="flex items-center gap-1.5 text-sm">
      {/* pill nav items with premium focus */}
    </nav>
  </div>
</header>
